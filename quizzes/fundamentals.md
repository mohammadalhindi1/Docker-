# Docker Fundamentals Quiz

## Questions

1. What is the difference between an image and a container?
2. What does `docker run` do that `docker start` does not?
3. Does a container normally include its own kernel?
4. What keeps a container running?
5. What is an image layer?
6. What is the build context?
7. Why use `.dockerignore`?
8. What is the difference between `CMD` and `ENTRYPOINT`?
9. Does `EXPOSE` publish a port?
10. Explain `8080:80`.
11. Why should persistent data use a volume?
12. When is a bind mount useful?
13. What does `localhost` mean inside a container?
14. How do containers resolve names on a user-defined bridge?
15. What does Docker Compose define?
16. Does `depends_on` always mean the dependency is ready?
17. Why use a multi-stage build?
18. What is dangerous about mounting the Docker socket?
19. Why is the `latest` tag not enough for reproducibility?
20. Which commands would you run first when a container exits unexpectedly?

Write answers before checking [the answer key](answers.md).
